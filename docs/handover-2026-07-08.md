# Session handover — 2026-07-08

Big session. Migrated email off Stalwart onto **mailcow**, cut DNS over, added
per-mailbox billing + 5 metrics, and made email customer-self-service. All code is
committed + pushed to `main`; the panel was redeployed (`jhg9z1k876014089se8h54x1`).

## 1. Mail engine: Stalwart → mailcow (DONE)

**Why:** Stalwart gates multi-tenant isolation behind paid Enterprise; requirement was
fully-free → mailcow (GPL-3, batteries-included). Spec: `docs/superpowers/specs/
2026-07-07-mailcow-migration-plan.md`.

**Live state:**
- mailcow runs on **`debut-mail-2`** (Hetzner id `148869435`, cpx→cx33 8GB) on the
  **warmed IP `46.224.111.182`** (moved from the old Stalwart box via a primary-IP swap).
  Primary IP id `139270478`, `auto_delete=false` (protected).
- Hostname **`mail.debutdepoly.com`**, valid **Let's Encrypt** cert. Webmail = SOGo at
  `https://mail.debutdepoly.com/SOGo`. SMTP(587)/IMAPS(993) verified working.
- **Stalwart is decommissioned** — old box `debut-mail-1` (148193641) DELETED + its orphan
  temp IP deleted. No rollback box remains (mailcow verified before deletion).
- Panel talks to mailcow via env **`MAILCOW_API_URL=https://mail.debutdepoly.com`** +
  **`MAILCOW_API_KEY`** (set in Coolify; `STALWART_*` now unused/ignored).
- SSH to the mailcow box: key at `…/scratchpad/mailcow_key` (root@46.224.111.182).
  Install lives in `/opt/mailcow-dockerized`. Recreate config change with `docker compose up -d`.

**Domains + mailboxes in mailcow (recreated fresh, each with DKIM):**
`debutdepoly.com`, `trustedcarfinance.com`, `debutwebconsultants.co.uk`,
`luxuryescortsmanchester.co.uk`. Mailboxes: `admin@debutdepoly.com`,
`paul@debutwebconsultants.co.uk`, `pf@trustedcarfinance.com`. (Passwords generated this
session — see the transcript; rotate.)

**Code:** `server/mail.js` rewritten Stalwart-JMAP → mailcow REST (same exported fn names,
so routes/UI unchanged); DKIM fetched from mailcow; SPF is self-host (`v=spf1 mx`).
`dnsRecords()` kept sync so Domain Connect still works. Tests: `server/mail.test.js`.

## 2. DNS cutover (mostly DONE)

Switched MX/SPF/DKIM/DMARC/autoconfig to mailcow via the **GoDaddy API** (key gated to
10+-domain accounts, which this is) for the 3 in-account domains — verified. Preserved
non-mail records (Facebook/Google/TikTok verification TXTs, app/www A records).

**OUTSTANDING:** `trustedcarfinance.com` is registered **elsewhere** (GoDaddy API 403) —
its records were printed in the transcript and need adding at whatever DNS host it uses.

## 3. Billing: per-mailbox email charge (DONE)

- Migration 25: `mail_domains(domain→org_id)` + `mail_mailboxes(address→org_id)`.
  Mail routes keep them in sync on create/delete.
- `billing.js computeMonthlyCharge` adds a **GBP-native £2.99/mailbox** line (comp/discount
  applies). `MAIL_PLANS` in `plans.js` (pricePence 299). Shown on Billing matrix.
- Tests: `server/billing_mail.test.js`, `server/plans.test.js`.
- NOTE: the 3 existing mailboxes were created directly via API (not the panel) so they're
  NOT in `mail_mailboxes` → not billed. They're the operator's own domains anyway (fine).

## 4. Metrics: +5 (DONE)

`server/metrics.js` + migration 24 (`metrics_samples` extra cols + `host_samples`). Added
disk I/O, PIDs, network **throughput** (Mbps from cumulative deltas), **uptime %**, and a
**host capacity** card (CPU/RAM/disk %, disk red >85%). `GET /api/metrics/host` (admin).
Shows on the service Metrics tab.

## 5. Email customer self-service (Part 1 DONE; Parts 2–3 OUTSTANDING)

- **DONE:** `/api/mail/*` dropped `requireAdmin` → `requireAuth + attachOrgContext`,
  **org-scoped** — a customer only sees/acts on their own org's domains/mailboxes
  (`assertMailDomainOrg`, cross-org → 404). Email is in the customer nav; new domains
  auto-bill to the customer's org. Webmail + IMAP/SMTP settings panel added (Email page).
- **OUTSTANDING — the user explicitly wants these two next:**
  - **Deploy from Git for customers** — surface `/new-git` UI to customers, route via the
    existing customer-capable `POST /api/apps`, behind the **billing gate** (card on file
    before first deploy — see `docs/superpowers/specs/2026-07-04-billing-gate-*` spec).
  - **Import from Render for customers** — verify the `/api/import/render/*` routes stamp
    resources to the caller's org, then expose the `/import` nav to customers.
  - Onboarding: new-account dashboard should offer all three (gated by billing setup).

## Also written this session (design specs, not yet built)

- `docs/superpowers/specs/2026-07-05-business-email-hosting-phase1-design.md` (SES-relay
  Phase-1 — SUPERSEDED by the mailcow migration for the engine; product/billing still valid).
- `docs/superpowers/specs/2026-07-05-fleet-capacity-autoscaling-design.md` (fleet
  capacity + Cluster-Autoscaler-style autoscaling — NOT built; peer-reviewed, ready to plan).
- `docs/superpowers/specs/2026-07-04-metrics-history-graphs-design.md` (metrics — built).

## MUST DO (operator)

1. **Rotate every secret shared in the transcript**: GoDaddy API key
   (`developer.godaddy.com/keys` — DELETE it), mailcow UI admin `nb15QIm76ul1jyCdux`,
   the mailbox passwords, root SSH password for 46.224.111.182, and the mailcow API key.
2. **`trustedcarfinance.com`** DNS records at its real host (transcript has them).
3. Smoke-test mail once DNS propagates (send to `admin@debutdepoly.com`, check SOGo).

## Fleet snapshot (Hetzner)

- `ubuntu-4gb-fsn1-3` (146096698) — Coolify host `167.233.206.184`
- `mflh-heavy` (147674558) — `157.90.244.221`
- `debut-mail-2` (148869435) — mailcow, `46.224.111.182`
