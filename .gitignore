node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# SQLite data + sessions (runtime state)
server/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# session scratch (browser dumps, screenshots, deploy pokers)
.playwright-mcp/
*.jpeg
premium-agent-hub-*.png
server/_tok.mjs
server/_deploydd.mjs
server/_dodeploy.mjs

services-full.png
.credentials.local

# local agent config — contains an API token
.codex/config.toml

# transient tool output — Playwright session dumps, screenshots, pid files.
# The site's real assets live in www/assets/img/; these are duplicate build artifacts.
.playwright-cli/
output/
